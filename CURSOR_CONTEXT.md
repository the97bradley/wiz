# Cursor / LLM context

Living context for agents and other LLMs working in this repository.
**Append** new findings here as work progresses; do not delete historical notes unless they are wrong.

Last updated: 2026-07-30 (Wiz MCP service-account headers work)

---

## Purpose of this repo

- GitHub: `the97bradley/wiz`
- Role: local mirror / workspace for public repositories from the **wiz-sec** GitHub organization (`https://github.com/wiz-sec`).
- Not an application monorepo; primary artifact is the set of git submodules under `wiz-sec/` plus sync tooling.

## Owner preferences (important)

- Prefer **pushing to `main`** over long-lived PR workflows when the user says so.
- Keep wiz-sec mirrors **up to date**: whenever the user re-runs the “pull/sync wiz-sec repos” prompt (including daily), run the sync script and commit updated submodule pointers.
- Continuously **grow this document** with durable context useful to future LLM sessions.

## Layout

```
.
├── AGENTS.md                 # Short agent runbook (sync steps)
├── CURSOR_CONTEXT.md         # This file — long-form living context
├── README.md                 # Human-facing overview
├── scripts/
│   └── sync-wiz-sec.sh       # Clone/add + fast-forward all wiz-sec public repos
├── wiz-sec/
│   ├── SYNC_STATUS.md        # Generated snapshot (date, SHAs, fork/archived flags)
│   └── <repo>/               # git submodules (see list below)
└── doc                       # Legacy empty placeholder from initial commit
```

## Sync workflow

Script: `./scripts/sync-wiz-sec.sh`

Requirements:

- `gh` (GitHub CLI) with API access to list `orgs/wiz-sec/repos`
- network + `git`

Behavior:

1. Lists **all public** repos in `wiz-sec` (including **archived**)
2. Adds missing repos as submodules under `wiz-sec/<name>` tracking each repo’s **default branch**
3. Updates existing submodules to `origin/<default_branch>` tip
4. Rewrites `wiz-sec/SYNC_STATUS.md`
5. Warns (does not delete) if a local submodule no longer exists upstream

After sync, agents should:

```bash
./scripts/sync-wiz-sec.sh
git add wiz-sec .gitmodules
git commit -m "Sync wiz-sec repos"
git push origin main   # when user wants main; otherwise follow branch/PR policy for the session
```

Env override: `WIZ_SEC_ORG` (default `wiz-sec`) if mirroring a different org name.

## Mirrored repos (as of 2026-07-30)

| Path | Upstream | Default branch | Notes |
|------|----------|----------------|-------|
| `wiz-sec/auth0` | wiz-sec/auth0 | master | archived; fork of Auth0 Go SDK |
| `wiz-sec/aws-vault` | wiz-sec/aws-vault | master | fork; AWS credential helper |
| `wiz-sec/azure-sdk-for-go` | wiz-sec/azure-sdk-for-go | master | fork; large Microsoft Azure SDK for Go |
| `wiz-sec/azure-service-bus-go` | wiz-sec/azure-service-bus-go | master | fork; Azure Service Bus Go client |
| `wiz-sec/charts` | wiz-sec/charts | master | native (not a fork); Helm charts |
| `wiz-sec/circle-monorepo` | wiz-sec/circle-monorepo | master | fork; CircleCI monorepo example |
| `wiz-sec/csvutil` | wiz-sec/csvutil | master | fork; CSV ↔ Go mapping |
| `wiz-sec/go-retryablehttp` | wiz-sec/go-retryablehttp | master | fork; retryable HTTP client |
| `wiz-sec/grammes` | wiz-sec/grammes | master | fork; Gremlin / TinkerPop Go client |
| `wiz-sec/open-cvdb` | wiz-sec/open-cvdb | main | native; open cloud vulnerability DB |
| `wiz-sec/orbs` | wiz-sec/orbs | master | fork; CircleCI orbs / swissknife jobs |

Canonical SHA snapshot: see `wiz-sec/SYNC_STATUS.md` (regenerated by sync).

## Technical notes

- Submodules are recorded in `.gitmodules` with explicit `branch = …` so `git submodule update --remote` tracks the right tip.
- `azure-sdk-for-go` is large; expect longer clone/fetch times.
- Nested `.git` dirs belong to submodules; do not vendor their contents into the parent tree.
- Org listing via `gh api orgs/wiz-sec/repos --paginate` (public API; no special org membership required for public repos).

## Session log

Add dated bullets below when something durable is learned (API quirks, repo purpose deep-dives, user decisions, gotchas).

### 2026-07-30

- Initial mirror: all 11 public wiz-sec repos added as submodules; sync script + README + AGENTS.md landed on `main`.
- User asked to keep repos current on each re-use of the sync prompt.
- User asked for this living Cursor context markdown for other LLMs; file created as `CURSOR_CONTEXT.md`.

### 2026-07-30 — Wiz MCP / cloud env secrets

Cloud Agent env injects four secrets (also listed in `CLOUD_AGENT_*_SECRET_NAMES`):

| Env var | Role | Observed shape (do not paste full secret values) |
|---------|------|--------------------------------------------------|
| `WIZ_MCP_AUTH` | GraphQL OAuth token URL | host `auth.test.wiz.io`, path `/oauth/token` |
| `WIZ_MCP_CLIENT_ID` | MCP / SA client id | base64-ish (~53 chars); **not** a UUID |
| `WIZ_MCP_CLIENT_SECRET` | MCP / SA secret | ~64-char secret (do not log) |
| `WIZ_MCP_ENDPOINT` | GraphQL API | host `api.eu3.test.wiz.io`, path `/graphql` |

Tenant DC derived from endpoint host: **`eu3`**. Env is test (`*.test.wiz.io`), not prod (`*.app.wiz.io`).

### 2026-07-30 — Working MCP hit (service-account headers)

Official Wiz MCP service-account auth is **not** Bearer against MCP. Send all three headers on every request:

- `Wiz-Client-Id` ← `WIZ_MCP_CLIENT_ID`
- `Wiz-Client-Secret` ← `WIZ_MCP_CLIENT_SECRET`
- `Wiz-DataCenter` ← `eu3` (from Tenant Info / `api.<dc>.…` host)

**Working URL (this tenant):** `https://mcp.test.wiz.io`  
(Prod docs say `https://mcp.app.wiz.io`; those creds return `Invalid client credentials` on prod.)

Verified flow:

1. `POST initialize` → 200, `mcp-session-id`, server `Wiz MCP Server` / `1.0.0`
2. `notifications/initialized` → 202
3. Gateway `tools/list` → `discover` + `execute`
4. `?mode=explicit` → 91 tools exposed directly
5. `execute` / `get_issue_counts` → live counts (e.g. total ~402k issues)

**Also works:** GraphQL via OAuth client_credentials. Use **form-urlencoded** (not JSON) to `WIZ_MCP_AUTH` with `audience=wiz-api`, then Bearer on `WIZ_MCP_ENDPOINT`. Bearer tokens do **not** authenticate the hosted MCP URL.

**Gotchas**

- Missing/invalid `Wiz-*` headers → silent fallback to browser OAuth (docs); from raw HTTP you see 401 `Invalid or expired token` instead.
- `mcp.eu3.test.wiz.io` returns `Invalid request` for initialize; use issuer host `mcp.test.wiz.io` with `Wiz-DataCenter: eu3`.
- No Wiz entry in Cursor MCP catalog here (`GetMcpTools`); hit over HTTP.
- Earlier false lead: `CLIENT_ID` once duplicated the auth URL — since fixed.

## How to extend this file

When finishing non-trivial work, append:

1. **What changed** (paths / behavior)
2. **Why** (user intent or constraint)
3. **Gotchas** for the next agent
4. Update **Last updated** at the top

Prefer short bullets over rewriting the whole doc.
